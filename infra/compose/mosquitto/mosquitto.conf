listener 1883
allow_anonymous true

# Only accept connections from within the Docker network.
# Caddy/Tailscale terminate all external access; this broker is LAN-only.
bind_address 0.0.0.0

persistence true
persistence_location /mosquitto/data/

log_dest stdout
log_type error
log_type warning
log_type notice
log_type information
